# Azure DevOps On-Premises MCP Server Configuration

# Azure DevOps Server URL including collection
# Example: https://tfs.company.com/DefaultCollection
ADO_SERVER_URL=https://your-server/DefaultCollection

# Personal Access Token (PAT) for authentication
# Generate from: https://your-server/DefaultCollection/_usersSettings/tokens
ADO_PAT_TOKEN=your-pat-token-here

# Optional: Logging level (error, warn, info, debug)
# LOG_LEVEL=info

# Optional: Disable TLS verification for self-signed certificates (USE ONLY IN DEV!)
# NODE_TLS_REJECT_UNAUTHORIZED=0
